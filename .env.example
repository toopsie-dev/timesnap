# Database file path — set this to a Railway volume mount path for persistence
# Example: /data/timesnap.db  (mount your Railway volume to /data)
# DATABASE_PATH=/data/timesnap.db

# Nuxt/Nitro will automatically use the PORT variable set by Railway
# PORT=3000
